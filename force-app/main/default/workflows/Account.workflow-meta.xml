<?xml version="1.0" encoding="UTF-8"?>
<Workflow xmlns="http://soap.sforce.com/2006/04/metadata">
    <fieldUpdates>
        <fullName>MAJNOM</fullName>
        <description>Met à jour le nom de famille selon ce modèle Xxxx</description>
        <field>LastName</field>
        <formula>IF( 
FIND(&quot; &quot;, LastName) != 0, 
UPPER(LEFT( LastName , 1)) 
+ LOWER(MID(LastName, 2,FIND(&quot; &quot;, LastName) - 1)) 
+ UPPER(MID(LastName, FIND(&quot; &quot;, LastName) + 1, 1)) 
+ LOWER(MID(LastName, FIND(&quot; &quot;, LastName) + 2, 80)), 
IF( 
FIND(&quot;-&quot;, LastName) != 0, 
UPPER(LEFT( LastName , 1)) 
+ LOWER(MID(LastName, 2,FIND(&quot;-&quot;, LastName) - 1)) 
+ UPPER(MID(LastName, FIND(&quot;-&quot;, LastName) + 1, 1)) 
+ LOWER(MID(LastName, FIND(&quot;-&quot;, LastName) + 2, 80)), 
UPPER(LEFT( LastName , 1)) + LOWER(MID(LastName, 2,80)) 
) )</formula>
        <name>MAJ NOM</name>
        <notifyAssignee>false</notifyAssignee>
        <operation>Formula</operation>
        <protected>false</protected>
    </fieldUpdates>
    <fieldUpdates>
        <fullName>MAJPrenom</fullName>
        <field>FirstName</field>
        <formula>IF( 
FIND(&quot; &quot;, FirstName) != 0, 
UPPER(LEFT( FirstName , 1)) 
+ LOWER(MID(FirstName, 2,FIND(&quot; &quot;, FirstName) - 1)) 
+ UPPER(MID(FirstName, FIND(&quot; &quot;, FirstName) + 1, 1)) 
+ LOWER(MID(FirstName, FIND(&quot; &quot;, FirstName) + 2, 80)), 
IF( 
FIND(&quot;-&quot;, FirstName) != 0, 
UPPER(LEFT( FirstName , 1)) 
+ LOWER(MID(FirstName, 2,FIND(&quot;-&quot;, FirstName) - 1)) 
+ UPPER(MID(FirstName, FIND(&quot;-&quot;, FirstName) + 1, 1)) 
+ LOWER(MID(FirstName, FIND(&quot;-&quot;, FirstName) + 2, 80)), 
UPPER(LEFT( FirstName , 1)) + LOWER(MID(FirstName, 2,80)) 
) )</formula>
        <name>MAJ Prénom</name>
        <notifyAssignee>false</notifyAssignee>
        <operation>Formula</operation>
        <protected>false</protected>
    </fieldUpdates>
    <fieldUpdates>
        <fullName>MAJ_NOM_de_famille</fullName>
        <field>LastName</field>
        <formula>IF( 
FIND(&quot; &quot;, LastName) != 0, 
UPPER(LEFT( LastName , 1)) 
+ LOWER(MID(LastName, 2,FIND(&quot; &quot;, LastName) - 1)) 
+ UPPER(MID(LastName, FIND(&quot; &quot;, LastName) + 1, 1)) 
+ LOWER(MID(LastName, FIND(&quot; &quot;, LastName) + 2, 80)), 
IF( 
FIND(&quot;-&quot;, LastName) != 0, 
UPPER(LEFT( LastName , 1)) 
+ LOWER(MID(LastName, 2,FIND(&quot;-&quot;, LastName) - 1)) 
+ UPPER(MID(LastName, FIND(&quot;-&quot;, LastName) + 1, 1)) 
+ LOWER(MID(LastName, FIND(&quot;-&quot;, LastName) + 2, 80)), 
UPPER(LEFT( LastName , 1)) + LOWER(MID(LastName, 2,80)) 
) )</formula>
        <name>MAJ NOM de famille</name>
        <notifyAssignee>false</notifyAssignee>
        <operation>Formula</operation>
        <protected>false</protected>
    </fieldUpdates>
    <fieldUpdates>
        <fullName>Update_ProduitUGC_To_FR</fullName>
        <field>ProduitUGC__c</field>
        <literalValue>français</literalValue>
        <name>Update ProduitUGC to FR</name>
        <notifyAssignee>false</notifyAssignee>
        <operation>Literal</operation>
        <protected>false</protected>
    </fieldUpdates>
    <rules>
        <fullName>ACCOUNT_MAJ des champs en majuscule</fullName>
        <actions>
            <name>MAJPrenom</name>
            <type>FieldUpdate</type>
        </actions>
        <actions>
            <name>MAJ_NOM_de_famille</name>
            <type>FieldUpdate</type>
        </actions>
        <active>true</active>
        <criteriaItems>
            <field>User.ByPassWorkflow__c</field>
            <operation>equals</operation>
            <value>False</value>
        </criteriaItems>
        <triggerType>onAllChanges</triggerType>
    </rules>
    <rules>
        <fullName>ACC_WF01_Change ProduitUGC FR</fullName>
        <actions>
            <name>Update_ProduitUGC_To_FR</name>
            <type>FieldUpdate</type>
        </actions>
        <active>true</active>
        <formula>AND (
$User.ByPassWorkflow__c = false,
 CodePays__c = &#39;033&#39;
)</formula>
        <triggerType>onAllChanges</triggerType>
    </rules>
</Workflow>
